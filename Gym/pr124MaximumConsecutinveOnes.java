
import java.util.*;

public class pr124MaximumConsecutinveOnes {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        int arr[] = new int[n];
        for (int i = 0; i < n; i++) {
            arr[i] = sc.nextInt();
        }
        // int maxcount = 0;
        // int curCount = 0;
        // for (int i = 0; i < n; i++) {
        // if (arr[i] == 1) {
        // curCount += 1;
        // } else {
        // maxcount = Math.max(maxcount, curCount);
        // curCount = 0;
        // }
        // }
        // maxcount = Math.max(maxcount, curCount);
        // System.out.println(maxcount);

        int max = 0;
    }

}